# requirements.txt
# This file tells Vercel which dependencies to install.

# The Django web framework is essential for your app to run.
django>=4.0

# Gunicorn is the WSGI server Vercel uses to execute your Django application.
# This is a critical dependency for Vercel deployments.
gunicorn

# If your project uses any other packages (e.g., pillow, psycopg2-binary),
# list them here as well.
